# Aeron — Luxury Car Landing

Cinematic single-page landing built with React + Vite + Tailwind CSS + Framer Motion.

## Local

```bash
npm install
npm run dev
```

Open the URL printed by Vite (default `http://localhost:5173`).

## Build

```bash
npm run build
npm run preview
```
